<Window x:Class="Wpf2._5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf2._5"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">


    <Grid>
        <!-- Style="ButtonStyle" -->
        <Button     
            Content="Отменить" 
            HorizontalAlignment="Center"
            Margin="0,187,0,0"
            VerticalAlignment="Top"
            RenderTransformOrigin="-1.021,-3.222" Style="{DynamicResource ButtonStyle}"
            />
        <Button Content="Специальная кнопка" 
                       Style="{StaticResource InteractiveButtonStyle}"
IsEnabled="{Binding IsChecked, ElementName=ToggleButton}"
                HorizontalAlignment="Center" Margin="0,282,0,0" VerticalAlignment="Top" Height="17" Width="142" RenderTransformOrigin="0.215,0.677"/>
        <TextBlock HorizontalAlignment="Center"
                   Margin="0,84,0,0" 
                   TextWrapping="Wrap" 
                   Text="Настройки приложения" 
                   VerticalAlignment="Top" Style="{DynamicResource HeaderTextStyle}" RenderTransformOrigin="0.453,0.668"
                   />
        <Button 
            Content="Сохранить" 
            HorizontalAlignment="Center" 
            Margin="0,145,0,0"   Style="{DynamicResource ButtonStyle}"
            VerticalAlignment="Top" 
            />
        <CheckBox x:Name="ToggleButton" Content="Включить режим" HorizontalAlignment="Center" Margin="0,243,0,0" VerticalAlignment="Top"
                 />


    </Grid>
</Window>
