<Window x:Class="Wpf2._9v6.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf2._9v6"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.CommandBindings>
        <CommandBinding Command="local:CustomCommands.ChangeColor" Executed="Button_Click" />
        <CommandBinding Command="Undo" Executed="UndoExecuted" />
    </Window.CommandBindings>
    <Grid>
        <DockPanel Grid.ColumnSpan="2" >
            <Menu  DockPanel.Dock="Top"  Name="Meinmenu" >
                <MenuItem Header="Действия">
                    <MenuItem Header="Изменить цвет"  Command="local:CustomCommands.ChangeColor"/>

                    <MenuItem Header="Отменить" Command="Undo"  />

                </MenuItem>

            </Menu>
        </DockPanel>
        <Button   x:Name="c" Content="Изменить цвет" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,249,0,0" 
                Background="#FF887C7C" Command="local:CustomCommands.ChangeColor"
            />
        <Button x:Name="CancelButton" Content="Отменить" HorizontalAlignment="Center" VerticalAlignment="Center" Background="#FF887C7C"
                Command="Undo"

             />


    </Grid>
</Window>
